function cm(n) {
	return `${n}cm`;
}

function mm(n) {
	return `${n}mm`;
}

function inch(n) {
	return `${n}in`;
}

function px(n) {
	return `${n}px`;
}

function pt(n) {
	return `${n}pt`;
}

function pc(n) {
	return `${n}pc`;
}

function em(n) {
	return `${n}em`;
}

function ex(n) {
	return `${n}ex`;
}

function ch(n) {
	return `${n}ch`;
}

function rem(n) {
	return `${n}rem`;
}

function vw(n) {
	return `${n}vw`;
}

function vh(n) {
	return `${n}vh`;
}

function vmin(n) {
	return `${n}vmin`;
}

function vmax(n) {
	return `${n}vmax`;
}

function percents(n) {
	return `${n}%`;
}

module.exports = { cm, mm, inch, px, pt, pc, em, ex, ch, rem, vw, vh, vmin, vmax, percents };
